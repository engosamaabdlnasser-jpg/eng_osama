# ENG OSAMA — Phase 2B

تمت إضافة طبقة أمان أولية لنظام دعم الطلاب، بدون تغيير مسارات الموقع أو حذف أي ميزة موجودة.

## ما تم إضافته

- `V14-SECURITY-AUDIT-RATE-LIMIT.sql`
- التحقق من رسائل الدعم:
  - منع الرسائل الفارغة.
  - حد أقصى 8000 حرف.
  - منع control characters غير المدعومة.
- Rate limiting على مستوى قاعدة البيانات للطالب:
  - 10 رسائل خلال الدقيقة.
  - 100 رسالة خلال 24 ساعة.
  - ردود الأدمن غير مشمولة في هذا الحد.
- `security_audit_logs` لتسجيل:
  - تغيير حالة المحادثة.
  - تغيير تعيين الأدمن.
  - تغيير استثناء الاحتفاظ.
  - إنشاء الرسائل.
- قراءة سجل التدقيق للأدمن فقط، ولا توجد صلاحية إدخال مباشرة من المتصفح.
- جميع قواعد الحماية تعمل داخل PostgreSQL triggers، لذلك لا يمكن تجاوزها بمجرد تعديل واجهة المستخدم.

## التشغيل

1. افتح Supabase SQL Editor.
2. شغّل أولًا `V13-HUMAN-SUPPORT-FOUNDATION.sql` إذا لم يكن قد تم تشغيله.
3. شغّل `V14-SECURITY-AUDIT-RATE-LIMIT.sql` مرة واحدة.
4. نفّذ اختبارًا برسالة دعم عادية، ثم راجع جدول `security_audit_logs` من حساب أدمن.

## ما لم يتم تنفيذه بعد

هذه المرحلة لا تضيف AI أو Telegram أو Notifications أو Edge Scheduler جديد. كما أن اختبار RLS الفعلي يجب تنفيذه داخل مشروع Supabase نفسه بحساب طالب وحساب أدمن منفصلين.
