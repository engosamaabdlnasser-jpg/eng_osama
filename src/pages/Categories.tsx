import {useEffect,useState} from 'react'; import {Link} from 'react-router-dom'; import {getCategories,demoCategories} from '../services/data'; import type {Category} from '../types'; export default function Categories(){const[cats,setCats]=useState<Category[]>(demoCategories);useEffect(()=>{getCategories().then(setCats).catch(()=>{})},[]);return <section className="section"><div className="container"><h1 className="section-title">التصنيفات</h1><p className="muted" style={{marginBottom:28}}>مجالات تعليمية مرتبة لتصل لما تحتاجه بسرعة.</p><div className="grid" style={{gridTemplateColumns:'repeat(auto-fit,minmax(210px,1fr))'}}>{cats.map(c=><Link key={c.id} to={'/courses?category='+c.id} className="surface" style={{padding:24}}><strong>{c.name}</strong><div className="muted" style={{marginTop:8,fontSize:13}}>استكشف الكورسات ←</div></Link>)}</div></div></section>}
