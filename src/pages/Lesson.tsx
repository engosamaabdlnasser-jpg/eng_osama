import {CheckCircle2,ChevronRight} from 'lucide-react'; import {useEffect,useState} from 'react'; import {Link,useParams} from 'react-router-dom'; import {getCourse,getCompleted,markLessonComplete} from '../services/data'; import {youtubeEmbedUrl} from '../utils/youtube'; import type {Course} from '../types'; import {supabase} from '../lib/supabase';
export default function Lesson(){const{courseId,lessonId}=useParams();const[course,setCourse]=useState<Course|null>(null);const[done,setDone]=useState(false);const[busy,setBusy]=useState(false);useEffect(()=>{if(courseId)getCourse(courseId).then(setCourse).catch(()=>{});if(lessonId&&supabase)supabase.auth.getUser().then(({data})=>{if(data.user)getCompleted(data.user.id).then(s=>setDone(s.has(lessonId))).catch(()=>{})})},[courseId,lessonId]);const lesson=course?.lessons?.find(x=>x.id===lessonId);if(!course||!lesson)return <div className="container section"><div className="surface empty">جاري تحميل الدرس أو الدرس غير موجود.</div></div>;const embed=youtubeEmbedUrl(lesson.youtube_url);async function complete(){if(!supabase){setDone(true);return}setBusy(true);try{const {data}=await supabase.auth.getUser();if(data.user){await markLessonComplete(data.user.id,lesson.id);setDone(true)}}finally{setBusy(false)}}return <section className="section"><div className="container"><Link to={`/courses/${course.id}`} className="muted" style={{display:'inline-flex',gap:5,alignItems:'center',marginBottom:20}}><ChevronRight size={16}/> العودة للكورس</Link><div className="surface" style={{overflow:'hidden'}}><div style={{background:'#020617',aspectRatio:'16/9'}}>{embed?<iframe src={embed} title={lesson.title} style={{width:'100%',height:'100%',border:0}} allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" allowFullScreen/>:<div style={{height:'100%',display:'grid',placeItems:'center',color:'#fff'}}>رابط YouTube غير صالح.</div>}</div><div style={{padding:24}}><div className="tag">الدرس {lesson.sort_order}</div><h1 style={{fontSize:30,margin:'10px 0'}}>{lesson.title}</h1><p className="muted" style={{lineHeight:2}}>{lesson.description}</p><button className="btn btn-primary" disabled={busy} onClick={complete} style={{marginTop:12}}>{done?<><CheckCircle2 size={18}/> تم إكمال الدرس</>:busy?'جاري الحفظ...':'تحديد كدرس مكتمل'}</button>{!supabase&&<div className="notice" style={{marginTop:14}}>وضع المعاينة مفعل. اربط Supabase لتخزين التقدم فعليًا.</div>}</div></div></div></section>}
