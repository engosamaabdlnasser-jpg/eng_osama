export function extractYouTubeVideoId(input:string):string|null{try{const raw=input.trim();const url=new URL(raw.startsWith('http')?raw:`https://${raw}`);const host=url.hostname.replace(/^www\./,'').toLowerCase();if(host==='youtu.be')return url.pathname.split('/').filter(Boolean)[0]??null;if(host==='youtube.com'||host==='m.youtube.com'||host==='youtube-nocookie.com'){if(url.pathname==='/watch')return url.searchParams.get('v');const parts=url.pathname.split('/').filter(Boolean);if(parts[0]==='embed'||parts[0]==='shorts')return parts[1]??null;}return null}catch{return null}}
export function youtubeEmbedUrl(input:string):string|null{const id=extractYouTubeVideoId(input);return id?`https://www.youtube.com/embed/${encodeURIComponent(id)}`:null}
